import 'package:flutter/material.dart';

class AppColors {
  // Primary theme colors based on the logo and terracotta theme
  static const Color primaryColor = Color(
    0xFFE2725B,
  ); // Vibrant terracotta orange
  static const Color secondaryColor = Color(0xFF2980B9); // Accent blue
  static const Color bgColor = Colors.white;
  static const Color textColor = Color(0xFF333333);
  static const Color hintTextColor = Colors.grey;
  static const Color borderColor = Color(
    0xFFDDDDDD,
  ); // For highlights if needed
  // Additional colors
  static const Color lightBackground = Color(0xFFF8F8F8);
  static const Color primaryText = Colors.white;
  static const Color secondaryText = Color(0xFF555555);
  static const Color textFieldFillColor = Color(
    0xFFF5F5F5,
  ); // Light grey for text fields
  static const Color primary = Color(0xFFE4572E); // Terracotta
  static const Color accent = Color(0xFFFFC107); // Bright Yellow
  static const Color background = primary; // Full-screen BG
  static const Color textFieldFill = Color(0xFFF2F2F2); // Light grey fill
}
